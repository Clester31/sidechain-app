specVersion: "v1beta"
serviceId: "sidechain"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "sidechain-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
